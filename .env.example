# Burn Rate Calendar - Environment Variables
# Copy this file to .env.local and fill in the values

# =============================================================================
# ENCRYPTION KEY (REQUIRED)
# =============================================================================
# Used to encrypt sensitive data like Monobank tokens at rest.
# Generate with: openssl rand -hex 32
# 
# In production, fetch this from HashiCorp Vault:
#   vault kv get -field=encryption_key secret/burn-rate-calendar
#
ENCRYPTION_KEY=

# =============================================================================
# NEXTAUTH (REQUIRED for production)
# =============================================================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Your production URL (without trailing slash)
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# DATABASE (REQUIRED)
# =============================================================================
# PostgreSQL connection string
# For Railway: Use the DATABASE_URL from Railway's PostgreSQL plugin
# For local development: postgresql://user:password@localhost:5432/burn_rate
DATABASE_URL=postgresql://postgres:password@localhost:5432/burn_rate

# =============================================================================
# HASHICORP VAULT (Optional - for production key management)
# =============================================================================
# VAULT_ADDR=https://vault.example.com:8200
# VAULT_TOKEN=hvs.xxxxxxxxxxxxx
# VAULT_PATH=secret/data/burn-rate-calendar

# =============================================================================
# SENTRY (Optional - for error tracking)
# =============================================================================
# Get your DSN from: https://sentry.io/settings/projects/burn-rate-calendar/keys/
# SENTRY_DSN=https://abc123@o123456.ingest.sentry.io/654321
